<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Clinic', root_path, class: 'navbar-brand link-info' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: 'nav-link active' %>
        </li>
        <% if patient_signed_in? || doctor_signed_in? %>
        <li class="nav-item">
          <%= link_to "Appointments", appointments_path, method: :get, class: 'nav-link active' %>
        </li>
        <% end %>
        <% if patient_signed_in? %>
        <li class="nav-item">
          <%= link_to "Doctors", new_appointment_path, method: :get, class: 'nav-link active' %>
        </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if patient_signed_in? %>
        <li class="nav-item">
          <%= button_to "Log out", destroy_patient_session_path, method: :delete, class: 'btn btn-outline-light me-2' %>
        </li>
        <% elsif doctor_signed_in? %>
        <li class="nav-item">
          <%= button_to "Log out", destroy_doctor_session_path, method: :delete, class: 'btn btn-outline-light me-2' %>
        </li>
        <% else %>
        <li class="nav-item">
          <%= button_to "Log in", new_patient_session_path, method: :get, class: 'btn btn-outline-light me-2' %>
        </li>
        <li class="nav-item">
          <%= button_to "Sign up", new_patient_registration_path, method: :get, class: 'btn btn-warning me-2' %>
        </li>
        <li class="nav-item">
          <%= button_to "I'm a doctor", new_doctor_session_path, method: :get, class: 'btn btn-info me-2' %>
        </li>
        <% end %>
      </ul>
  </div>
</nav>
